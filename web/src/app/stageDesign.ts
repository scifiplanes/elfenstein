/** Authoring size (CSS px): `FixedStageViewport`, `FramePresenter` / WebGL canvas, and HUD capture must match. */
export const STAGE_CSS_WIDTH = 1920
export const STAGE_CSS_HEIGHT = 1080

/** White frame on the **`.clip`** (inset `outline`); viewport fit uses **1920×1080** only. */
export const STAGE_OUTLINE_PX = 3
