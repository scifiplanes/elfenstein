/** Opts a control into `Hand_Active` when hovered without `data-drop-kind` (e.g. title/settings buttons). */
export const CURSOR_HAND_ACTIVE_ATTR = 'data-cursor-hand-active' as const
export const CURSOR_HAND_ACTIVE_SELECTOR = '[data-cursor-hand-active]' as const
