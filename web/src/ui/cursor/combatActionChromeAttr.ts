/** Combat bottom action cluster: block world `data-drop-kind` hit-through for cursor + viewport guards. */
export const COMBAT_ACTION_CHROME_ATTR = 'data-combat-action-chrome' as const
export const COMBAT_ACTION_CHROME_SELECTOR = '[data-combat-action-chrome]' as const
